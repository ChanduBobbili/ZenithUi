import BasicToast from "@/components/toast/toast/render-toast"
import ToastsExample from "@/components/toast/toast/toasts"
import PromiseToast from "@/components/toast/toast/promise-toast"
import ActionToast from "@/components/toast/toast/action-toast"
import ToastAnimations from "@/components/toast/toast/animations"
import { Table } from "nextra/components"

# Toast

## Rendering the toast

You can call it with just a string, that will become the title.

<BasicToast />

---

## Success

Renders a checkmark icon in front of the message.

<ToastsExample type="s" />

---

## Error

Renders an error icon in front of the message.

<ToastsExample type="e" />

---

## Warning

Renders a warning icon in front of the message.

<ToastsExample type="w" />

---

## Info

Renders a checkmark info in front of the message.

<ToastsExample type="i" />

---

## Loading

Renders a loading icon in front of the message.

<ToastsExample type="l" />

---

## Promise

You can provide promise to the toast. customize the text based the promise state `loading` , `success`, `error`.

<PromiseToast />

---

## Animations

You can provide animation prop to the toast.

<ToastAnimations />

---

## Actioned Toast

You provide the callback for when `action-btn`, `cancel-btn` and `close-btn` is clicked.

### onAction

<ActionToast type="action" />

### onCancel

<ActionToast type="cancel" />

### onClose

<ActionToast type="close" />

---

## showCloseButton

Renders a close button to dismiss the toast.

<ToastsExample type="w-c" />

## 🛠 API Reference

<Table>
  <thead>
    <Table.Tr>
      <Table.Th>Prop Name </Table.Th>
      <Table.Th>Type</Table.Th>
      <Table.Th>Description</Table.Th>
      <Table.Th>Default</Table.Th>
    </Table.Tr>
  </thead>
  <tbody>
    <Table.Tr>
      <Table.Td>**position**</Table.Td>
      <Table.Td>`ToastPosition`</Table.Td>
      <Table.Td>The position of the toast.</Table.Td>
      <Table.Td>`bottom-right`</Table.Td>
    </Table.Tr>

    <Table.Tr>
      <Table.Td>**richColors**</Table.Td>
      <Table.Td>`boolean`</Table.Td>
      <Table.Td>
        Whether to use rich colors for the toast.
      </Table.Td>
      <Table.Td>`false`</Table.Td>
    </Table.Tr>

    <Table.Tr>
      <Table.Td>**animation**</Table.Td>
      <Table.Td>`"fade" | "slide"`</Table.Td>
      <Table.Td>
       The animation of the toast.
      </Table.Td>
      <Table.Td>`"fade"`</Table.Td>
    </Table.Tr>

     <Table.Tr>
      <Table.Td>**duration**</Table.Td>
      <Table.Td>`number`</Table.Td>
      <Table.Td>
      The duration of the toast to display.
      </Table.Td>
      <Table.Td>`5 seconds`</Table.Td>
    </Table.Tr>

    <Table.Tr>
      <Table.Td>**disableAutoDismiss**</Table.Td>
      <Table.Td>`boolean`</Table.Td>
      <Table.Td>
        Whether to enable auto dismiss for the toast.
      </Table.Td>
      <Table.Td>`true`</Table.Td>
    </Table.Tr>

    <Table.Tr>
        <Table.Td>**showCloseButton**</Table.Td>
        <Table.Td>`boolean`</Table.Td>
        <Table.Td>
            Whether to show the close button for the toast.
        </Table.Td>
        <Table.Td>`false`</Table.Td>
    </Table.Tr>

  </tbody>
</Table>
