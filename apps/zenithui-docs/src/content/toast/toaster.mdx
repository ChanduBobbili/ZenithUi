import ToasterPosition from "@/components/toast/toaster/position"
import ToasterRichColors from "@/components/toast/toaster/rich-color"
import ToasterAnimation from "@/components/toast/toaster/animation"
import ToasterQueue from "@/components/toast/toaster/queue-system"
import { Table } from "nextra/components"

# Toaster

## Position

<ToasterPosition />

---

## Rich Colors

<ToasterRichColors />

---

## Animation

<ToasterAnimation />

---

## Queue System

When the number of active toasts exceeds `maxCount`, any additional toasts are placed in a queue. As soon as an active toast is dismissed, the next toast in the queue takes its place. By default, `maxCount` is set to 5.

<ToasterQueue />

---

## Offset

You can customize the toast's position by adding `X_Offset` and `Y_Offset`.

---

## 🛠 API Reference

<Table>
  <thead>
    <Table.Tr>
      <Table.Th>Prop Name </Table.Th>
      <Table.Th>Type</Table.Th>
      <Table.Th>Description</Table.Th>
      <Table.Th>Default</Table.Th>
    </Table.Tr>
  </thead>
  <tbody>
    <Table.Tr>
      <Table.Td>**position**</Table.Td>
      <Table.Td>`ToastPosition`</Table.Td>
      <Table.Td>The position of the toast.</Table.Td>
      <Table.Td>`bottom-right`</Table.Td>
    </Table.Tr>

    <Table.Tr>
      <Table.Td>**richColors**</Table.Td>
      <Table.Td>`boolean`</Table.Td>
      <Table.Td>
        Whether to use rich colors for the toast.
      </Table.Td>
      <Table.Td>`false`</Table.Td>
    </Table.Tr>

    <Table.Tr>
      <Table.Td>**animation**</Table.Td>
      <Table.Td>`"fade" | "slide"`</Table.Td>
      <Table.Td>
       The animation of the toast.
      </Table.Td>
      <Table.Td>`"fade"`</Table.Td>
    </Table.Tr>

     <Table.Tr>
      <Table.Td>**duration**</Table.Td>
      <Table.Td>`number`</Table.Td>
      <Table.Td>
      The duration of the toast to display.
      </Table.Td>
      <Table.Td>`5 seconds`</Table.Td>
    </Table.Tr>

    <Table.Tr>
      <Table.Td>**disableAutoDismiss**</Table.Td>
      <Table.Td>`boolean`</Table.Td>
      <Table.Td>
        Whether to enable auto dismiss for the toast.
      </Table.Td>
      <Table.Td>`true`</Table.Td>
    </Table.Tr>

    <Table.Tr>
        <Table.Td>**enableQueueSystem**</Table.Td>
        <Table.Td>`boolean`</Table.Td>
        <Table.Td>
           If too many toasts appear at once, we should queue them instead of overwhelming the user.
        </Table.Td>
        <Table.Td>`false`</Table.Td>
    </Table.Tr>

    <Table.Tr>
        <Table.Td>**maxToasts**</Table.Td>
        <Table.Td>`number`</Table.Td>
        <Table.Td>
           The maximum no of toasts to show when queue system is enabled.
        </Table.Td>
        <Table.Td>`5`</Table.Td>
    </Table.Tr>

    <Table.Tr>
        <Table.Td>**showCloseButton**</Table.Td>
        <Table.Td>`boolean`</Table.Td>
        <Table.Td>
            Whether to show the close button for the toast.
        </Table.Td>
        <Table.Td>`false`</Table.Td>
    </Table.Tr>

    <Table.Tr>
      <Table.Td>**theme**</Table.Td>
      <Table.Td>`"light" | "dark" | "auto"`</Table.Td>
      <Table.Td>Theme of the toast.</Table.Td>
      <Table.Td>`"auto"`</Table.Td>
    </Table.Tr>

  </tbody>
</Table>
